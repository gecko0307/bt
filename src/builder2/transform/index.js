const scripts = require("./scripts");
const styles = require("./styles");
const assets = require("./assets");
const prepare = require("./prepare");
const strip = require("./strip");

module.exports = {
    scripts, styles, assets, prepare, strip
};
